const DEFAULT_REGION = 'eu-west-1';

const GLOBAL_COMMANDS = ['jest', 'eslint']; // TODO what about all kinds of other commands?

const ALWAYS_TO_IGNORE = ['.git', 'node_modules', 'coverage'];

module.exports = {
    DEFAULT_REGION,
    GLOBAL_COMMANDS,
    ALWAYS_TO_IGNORE,
};
